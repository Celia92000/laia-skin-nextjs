const fs = require('fs');
const path = require('path');

async function importGiftCards(organizationId, prisma) {
  console.log('  ⚠️  Import des cartes cadeaux - À implémenter');
  return { imported: 0, errors: 0 };

  // TODO: Implémenter l'import des cartes cadeaux
}

module.exports = importGiftCards;
