const fs = require('fs');
const path = require('path');

async function importProducts(organizationId, prisma) {
  console.log('  ⚠️  Import des produits - À implémenter');
  return { imported: 0, errors: 0 };

  // TODO: Implémenter l'import des produits
}

module.exports = importProducts;
